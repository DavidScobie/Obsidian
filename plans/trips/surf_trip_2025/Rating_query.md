These breaks have 9<=Rating<10 :
```dataview
LIST location
WHERE (Rating >= 9) AND (Rating < 10)
```

These breaks have 8<=Rating<9 :
```dataview
LIST location
WHERE (Rating >= 8) AND (Rating < 9)
```

These breaks have 7<=Rating<8 :
```dataview
LIST location
WHERE (Rating >= 7) AND (Rating < 8)
```

These breaks have 6<=Rating<7 :
```dataview
LIST location
WHERE (Rating >= 6) AND (Rating < 7)
```

These breaks have 5<=Rating<6 :
```dataview
LIST location
WHERE (Rating >= 5) AND (Rating < 6)
```

These breaks have 4<=Rating<5 :
```dataview
LIST location
WHERE (Rating >= 4) AND (Rating < 5)
```

These breaks have Rating<4 :
```dataview
LIST location
WHERE (Rating >= 0) AND (Rating < 4)
```


